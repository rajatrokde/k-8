apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: nginx
spec:
  type: NodePort
  ports:
  - name: "nginx"
    port: 80
   # targetPort: 80
    NodePort: 30007 # specify your desired NodePort between 30000-32767
  selector:
    app: nginx
